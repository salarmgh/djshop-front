import React from "react";
const persianNumber = require("persian-number");

const Price = (props: {price: number}) => (
              {persianNumber.convertEnToPe(
              persianNumber.sliceNumber(props.price)
            )}
)
