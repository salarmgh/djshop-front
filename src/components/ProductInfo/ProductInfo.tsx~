import React from "react";

const ProductInfo = (props: {
  id: number;
  images: [
    {
      id: number;
      image: string;
      title: string;
    }
  ];
  name: string;
  product: {
    id: number;
    title: string;
    description: string;
    created_at: string;
    featured: boolean;
    slug: string;
  };
  price: number;
}) => { };
